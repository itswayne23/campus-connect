[build]
  command = "npx tsc --noEmit --skipLibCheck && npm run build"
  directory = "campus-connect/frontend"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
