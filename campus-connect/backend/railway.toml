[build]
  builder = "python"
  buildCommand = "pip install -r requirements.txt"
  startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[deploy]
  region = "us-west"
  numInstances = 1

[environment]
  NODE_ENV = "production"
